# Project Memory
> Auto-generated by /remember. Do not edit manually.

## Facts
- M0 (real passport scan, twice, verify SOD, derive matching zktag) is the top blocker for zkagent — nothing past it is evidence, only design.
- `grep` on this host is ugrep 7.5.0 — bounded-repetition regexes fail slowly and look like a hang; parse structured data with python3 instead.
- Never write custom passport-chip parsing code — wrap a vetted library (NFCPassportReader on iOS, JMRTD on Android/desktop).
- zkagent is Android-first: builds on the owner's Fedora box, no Mac/paid Apple account needed; iOS is an explicit non-goal for now.
- M0/M2+ Android dev device must be a Pixel, stock ROM, bootloader locked — StrongBox + Play Integrity + patch runway decide it, not NFC support.
- Never buy a custom-ROM (GrapheneOS etc.), FRP-locked, dead-display, or Pixel 3a/4a device for zkagent — each fails a hard requirement (Play Integrity, setup, or EOL).
- CPU/secure-enclave material must never enter zktag/identity derivation — it roots custody only, never identity, or "tag survives device change" breaks.
- `ok`/`allowed` must stay separate everywhere in this codebase: `ok:false` forces `allowed:null` — a broken verifier must never affirmatively answer "no".
- Real passport-derived material must never enter the git tree; keep `fixtures/real/`, `*.mrz`, `*.dg1`, `*.sod`, `secrets/` gitignored.
- Commit only when the owner explicitly asks — do not commit PRD/CHANGELOG/doc edits proactively.
- Don't blind-replace `tag`→`zktag` — Apple's "NFC tag-reading entitlement" and RFC 9421's own `tag` param must stay as `tag`.
- The npm placeholder `zkagent@0.0.0` is mislicensed as MIT (should be Apache-2.0); gated by NO-GO #8, not urgent, but do not forget it.
- `main`'s branch protection (1 review + linear history) can't be satisfied by a sole maintainer — GitHub blocks self-approval, forcing admin bypass on every merge.
- Repeated admin/direct-push bypasses of `main` protection are unresolved and unapproved — flag again each time, don't treat prior bypasses as standing consent.
- When a stale/broken reference is found after a docs reorg, fix it on sight or flag it — do not assume it's already correct.
- zkagent's agent-delegation layer (rung 2, M4-M5) is frozen until age-verification (rung 1, M0-M3) ships — don't design/discuss it, even if asked casually.
- No feature enters a zkagent milestone unless it is first written into the PRD (NO-GO #10, the scope gate).

## Episodes
### 2026-07-26 — Founding session
- goal: Decide what to build after an open-ended "next unmet need" strategy discussion.
- tried: Converged on zkagent — one anonymous deterministic tag per service derived from a passport NFC scan, agents acting under revocable delegation certs tied to that tag.
- outcome: v1 scope locked as captcha-grade (no ZK circuits), D1-D8 signed off, D9 (derivation field) deliberately left open pending M0 evidence, repo/PRD/npm placeholder created; blocked immediately on confirming an Apple NFC entitlement since the owner only had an iPhone.
- lesson: Riskiest assumption (issuer-free derivation) is unproven in the literature — treat it as unproven and let M0 evidence, not argument, resolve it.

### 2026-07-30 — De-platforming M0
- goal: Unblock M0 without a Mac or paid Apple developer account.
- tried: De-platformed M0 to a USB PC/SC NFC reader (ACR122U recommended) + JMRTD on the owner's Linux desktop as the primary path.
- outcome: PRD bumped to v1.1 (uncommitted) with de-platformed M0 row and conditional M1 fixture plan; a NL hardware shopping list was produced.
- lesson: This session's own ACR122U recommendation was wrong and got overturned the very next day — verify hardware claims (APDU size limits, protocol type) against the actual use case, not against a loosely related compatibility signal like "works for DigiD."

### 2026-07-31 — Android-first hardware decision
- goal: Re-evaluate the M0 hardware plan after the ACR122U recommendation failed technical scrutiny.
- tried: Falsified the ACR122U (261-byte APDU cap, can't read ICAO e-documents); decided to buy an Android phone instead of a reader, establishing the real device requirement set (StrongBox, Play Integrity 12-month patch window, stock ROM) rather than "NFC + recent Android."
- outcome: Locked target Pixel 8a > 7a > 6a; ruled out Pixel 3a/4a, custom ROMs, and FRP-locked/dead-display units; identified Marktplaats as the only viable NL used-phone market (Vinted's cheap stock is drop-damaged).
- lesson: Cheap-and-recent isn't a valid filter for this hardware — support-runway math (security patch EOL + 12mo strong-integrity grace) is the real constraint, and it eliminates devices that otherwise look fine.

### 2026-08-03 — PRD v1.3, zktag rename, attestation rethink, 8een overlap
- goal: Continue the hardware hunt and resolve accumulating design debt in the PRD.
- tried: Verified RFC 9421 assumptions against the real spec (found a `tag` naming collision, no delegation-chain mechanism, expiry precedence and nonce overlap issues); renamed `tag`→`zktag`; adopted several decisions borrowed from the sibling project 8een (configurable threshold/one-bit output, 30-day enclave-enforced secret expiry); reopened the attestation root as Q14; analyzed 8een overlap and decided to keep zkagent a separate project; sketched a "borrowable core, trust list held by adopters" repositioning.
- outcome: PRD reached v1.3 (uncommitted); FR6 (uniform payload shape) was found incompatible with the borrowable-core model and left as the single open question blocking further restructuring; parked an unrelated optical/air-gap transfer research thread.
- lesson: Design-only sessions accumulate unresolved surface faster than they resolve it — the PRD itself was flagged as becoming the "collector" its own NO-GO #10 warns against, for a project with zero lines of code and no M0 evidence yet.

### 2026-08-25 — PRD v1.4 disclosure modes, chiproof, device purchase, docs reorg
- goal: Resolve the FR6 deadlock and close out several other loose ends before M0 can finally run.
- tried: Checked the EU Age Verification Blueprint and split disclosure into Mode A (anonymous, unlinkable) / Mode B (pseudonymous, zktag) to resolve FR6; reserved and published the `chiproof` npm package; ran `/docs-builder reorg` twice, moving all docs under `docs/product/` and `docs/logs/`; owner bought a Pixel 6a satisfying the D2 device requirements.
- outcome: PRD merged as v1.4 (committed/pushed); discovered a new chip-cloning threat (Q18) that undermines D10's 30-day-expiry rationale for Mode B; found an unresolved partial/broken link fix in `packages/chiproof/README.md` from an external edit; flagged three branch-protection bypasses on `main` as unresolved; M0 still not run.
- lesson: More design work surfaces more risk rather than more certainty — Q18 (chip cloning) sat undetected through four PRD revisions and was only found when the read path had to be written out concretely for a companion doc, which is exactly why rung-2 work is now frozen until rung-1 ships real evidence.

## Antigens
### High Confidence (loaded — applies every session)
- Never claim code is "done", "validated", "tested", "fixed", or "verified" without having actually run real verification (tests/regression/smoke run/end-to-end); state explicitly when something is unverified instead of asserting success. (evidence: 6 sessions across 5 projects — "did you validate all fixes and confirmed that no regression? btw, this is by far one of the worst sessions of zero trust of the amount of times you said you finished and validated then found shit")

### Medium Confidence (observing — not loaded)
_(none yet)_

### Low Confidence (needs more data)
_(none yet — 9 other candidate clusters from this run were single-session and are not tracked until they recur; see `.claude/remember/report.md`)_
