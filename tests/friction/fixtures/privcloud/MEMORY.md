# Project Memory
> Auto-generated by /remember. Do not edit manually.

## Facts
- Never revisit options the user explicitly declined (router HW, bridge mode, subnet routing, VLANs/Pi-hole, DDNS) - don't re-raise unprompted.
- AdGuard/DNS-level blocking can't stop YouTube ads (served from content hostnames) - handle per-device instead.
- federver's server-side menu stays a reduced option set with no path back to the laptop menu; always drive full menus from the laptop.
- Services installed on both laptop and server (Syncthing, Tailscale) need Start/Stop/Restart/Uninstall acting on both sides; server-only installs get server-only lifecycle.
- Standalone Docker containers (AdGuard, Syncthing) live outside docker-compose.yml - Start All/Stop All must explicitly include them alongside compose commands.
- "Stop" means fully stopped AND disabled from autostart (docker update --restart=no + docker stop) - no separate Suspend/Resume concept in this project.
- Every Extras install offers Uninstall with typed-name confirm, reverting system changes but keeping /opt/<service> config; core services are untouched by it.
- User prefers minimal friction: no unnecessary credential prompts, short error messages, consistent color conventions (yellow=command, red=destructive/stopped, green=running).
- The SSH-hop wrapper auto-pulls latest git code onto the server before running any step, so the server never runs stale code.
- Root-owned server changes need an interactive sudo password the Bash tool can't supply over SSH (no TTY) - give the user a copy-paste sudo block instead of running it directly.
- Server SSH must work both on LAN and away from home - probe the LAN IP first with a short ConnectTimeout, fall back to the Tailscale address.
- This project's filesystem (ext4) is case-sensitive - avoid case-variant duplicate paths, which cause duplicate imports/entries.
- Version bumps are their own commit (rename ## Unreleased to ## vX.Y.Z, bump the version in setup.sh + package.json) - only when the user explicitly asks, but always offer.
- Only commit/push when the user explicitly says so; write detailed narrative CHANGELOG entries matching existing style; PRDs live under docs/product/.
- Validate against the real, live mechanism before claiming something works - a syntax check (bash -n) or visual read is not verification.
- Stay surgical - don't over-investigate or chase adjacent leads the user didn't ask about.
- Responsive web UI is mandatory for this project's UIs.
- Bash gotcha: `func || true` swallows errors from everything inside func; `var=$(cmd)` under set -e aborts on failure unless guarded - don't assume set -e catches either.
- `systemctl show -p <X> --value` returns empty for properties not exposed via show (e.g. OnCalendar) - use `systemctl cat <unit>` for unit-file settings instead.
- Direct-to-main pushes with branch-protection bypass are this repo's accepted convention, not a bug to flag.
- LAN-HTTP service exposure (Tailscale is the access model, no TLS) and unpinned :latest + Watchtower auto-updates are deliberate accepted tradeoffs - don't re-flag as findings.
- True LAN lockdown needs binding containers to the tailnet IP - Docker's own iptables ACCEPT rules run ahead of firewalld, so zone rules don't actually gate Docker-published ports.
- By design there is exactly ONE Immich backup schedule; privcloud and federver menus both drive the same job, so Remove/Status never need a chooser.
- Standard verification flow for changes: implement -> POC -> /code-review -> /security -> fix -> update docs/changelog -> commit -> push (only on explicit request).
- Use `ssh -t user@host "sudo <cmd>"` for remote sudo - never `sudo ssh ...`, which runs ssh as local root with the wrong identity/trust.
- Tailscale on federver is a native dnf package, not a Docker container - Watchtower can't auto-update it; that's by design, not a gap to fix.
- AdGuard's upstream must be plain DNS (9.9.9.9/149.112.112.112, fallback 1.1.1.1/8.8.8.8), never DoH - DoH to Quad9 hits a Go HTTP/2 bug that silently drops queries.

## Episodes

### 2026-04-15 - Homelab AdGuard exploration + router evaluation
(source: 2026-04-15-homelab-adguard.md)
- Goal: decide whether to buy new router hardware and whether to add AdGuard Home DNS filtering to the homelab.
- Tried: evaluated OpenWrt Flint 2, Merlin ASUS, UniFi UDR against keeping the Ziggo Connect Box; drafted a standalone Docker test plan for AdGuard Home.
- Outcome: decided against any router purchase (Tailscale already solved the actual need); planned a one-week AdGuard test before baking it into privcloud as an Extras option.
- Lesson: the user's real pain point (remote access) was already solved by Tailscale, so several router-layer proposals were unnecessary scope creep.

### 2026-04-16 - Menu system overhaul (AdGuard, Syncthing, both-sides lifecycle)
(source: 2026-04-16-homelab-adguard-syncthing-menus.md)
- Goal: install AdGuard Home and Syncthing into the federver menu system, which snowballed into restructuring the entire laptop/server menu architecture.
- Tried: split menus into a full laptop menu and a reduced server menu, added both-sides install/lifecycle handling, an emergency-restart lever, unified status views, auto-pull-on-SSH-hop.
- Outcome: setup.sh grew from ~2079 to 4263 lines across 40+ commits; v0.3.0 tagged; AdGuard and Syncthing both installed as standalone containers with full lifecycle support.
- Lesson: consistent patterns (both-sides lifecycle, Status-first submenus, uniform Uninstall) prevent divergent one-off behaviors as the menu system grows.

### 2026-06-05 - Navidrome playlist cleanup and storage-status fixes
(source: 2026-06-05-navidrome-playlists-storage-status.md)
- Goal: fix broken disk-usage/USB-partition detection in Storage->Status and clean up a bloated Navidrome library (~513 stray imported playlists).
- Tried: fixed awk parsing and USB/internal drive filtering; archived and deleted stray playlist files and junk cover-art; edited the Navidrome sqlite DB directly via GLOB (case-sensitive) matching.
- Outcome: Navidrome reduced from ~513 to 17 clean playlists; all backups removed after user confirmation; final state validated with zero missing tracks.
- Lesson: Navidrome smart (.nsp) playlists query the whole library regardless of folder depth, while .m3u8 genre lists are explicit flat file lists - very different maintenance needs.

### 2026-06-05 - Roaming SSH fix, playlist fixes, Immich CLI upload rework
(source: 2026-06-05-roaming-ssh-navidrome-immich-upload.md)
- Goal: fix federver menu hanging when away from home, then continue Navidrome playlist tuning and fix a broken Immich upload flow.
- Tried: added a global ssh() wrapper with ConnectTimeout/keepalives and LAN-probe-then-Tailscale-fallback; fixed smart-playlist path-matching bugs; replaced a broken npm-based Immich upload with the official immich-cli Docker image.
- Outcome: six releases shipped (v0.8.4 to v0.8.9) in one session, all committed and pushed to main.
- Lesson: Navidrome stores library-relative paths for smart-playlist rules and won't rename an already-synced smart playlist on rule re-import - only deleting the DB row picks up a new name.

### 2026-06-05 - Storage mount bug hunt and backup/sync UX redesign
(source: 2026-06-05-storage-backup-ux.md)
- Goal: diagnose why Navidrome/FileBrowser wouldn't start after adding a new HDD, which cascaded into a broader storage and backup-UX overhaul.
- Tried: traced the failure to a stale duplicate /mnt/data fstab entry shadowing the correct ext4 device; hardened mount detection with Docker exit-code checks; redesigned backup copy UX with a plain-language prompt and tree preview.
- Outcome: shipped v0.7.5 and v0.8.0 plus several fixes; documented the UX redesign in docs/product/prd-backup-sync-ux.md.
- Lesson: a stub/autofs mountpoint can silently mask a misconfigured underlying device - verify the real /dev/* device via findmnt, not just `mountpoint -q`.

### 2026-06-08 - Immich backup feature buildout (v0.9.0-v0.9.9)
(source: 2026-06-08-immich-backup-feature.md)
- Goal: figure out where Immich backups go, then build a full no-downtime backup feature (live rsync + online pg_dumpall) with one-time and scheduled modes.
- Built shared backup functions driving both privcloud and federver menus, plus security fixes (.env chmod 600, API key off argv, quoting).
- User found a false-success bug: `_immich_backup_remove || true` masked a failed sudo, reporting "removed" when the schedule was still active - fixed with upfront `sudo -v` plus post-removal verification.
- Lesson: `func || true` swallowing errors inside functions is a recurring trap; must verify state actually changed, not just that the command ran.

### 2026-06-09 - Backup Status fixes, menu UX, rsync slimming (v0.9.10-v0.9.12)
(source: 2026-06-09-immich-backup-status-cleanup.md)
- Goal: fix "Schedule: ?" showing in Status; ended up restoring the user's actually-broken scheduled backup end to end.
- Found root cause via live SSH debugging: `systemctl show -p OnCalendar` returns empty - switched to `systemctl cat`; also discovered the timer didn't even exist (an old cron job had been doing it).
- First live re-run of the fixed schedule failed with "No space left on device" (shared USB backup drive 100% full); fixed the underlying cause too by adding anchored rsync excludes for regenerable cache/thumbnail dirs.
- Lesson: user was frustrated by "all good" claims that broke on first real run - verification must exercise the actual live mechanism, not just static syntax checks.

### 2026-06-09 - Backup menu flatten + full security audit (v0.9.13-v0.9.18)
(source: 2026-06-09-backup-menu-flatten-security-review.md)
- Goal: flatten privcloud's nested Backup submenu into a single flat menu, then run a full security audit.
- Audit found and fixed real issues (path-injection-class quoting gap, .env.example sentinel mismatch, .env umask gap, exec sg docker re-quoting bug) and a logic bug (cron-only backups not detected by Remove's guard).
- Two findings (LAN cleartext exposure, unpinned :latest + Watchtower) were explicitly accepted as deliberate tradeoffs rather than fixed.
- Lesson/decision: established "ONE Immich backup schedule by design" as a standing architectural fact.

### 2026-06-11 - Privileged one-time Download: root-owned source + non-writable dest (v0.9.19-v0.9.20)
(source: 2026-06-11-privileged-download-source-and-dest.md)
- Goal: let the user copy the root-owned Immich backup off federver to their laptop when either the source tree or destination mount required elevated privileges.
- Built a probe-then-escalate design: only use a privileged tar-over-SSH pipeline when source read or dest write actually needs it, otherwise keep the plain resumable rsync path.
- First hypothesis (source was the blocker) proved wrong on live test - the actual blocker was the destination USB mount being root-owned; had to add a second, independent privilege wall.
- Lesson: an agent's own "by design" framing can overstate reality - user corrected that root-ownership was an accidental consequence, not an intentional safety design.

### 2026-08-24 - AdGuard/Tailscale DNS fix and docs reorg
(source: 2026-08-24-adguard-tailscale-dns-fix-and-docs-reorg.md)
- Goal: diagnose browsing breakage while traveling and intermittent hangs at home, via live debugging against the real federver box rather than guessing.
- Found two compounding root causes: a third-party VPN app blocking Tailscale's WireGuard/DERP traffic while traveling, and AdGuard's DoH upstream to Quad9 hitting an idle-connection-reuse bug.
- Fixed AdGuard's `step_adguard` Docker idempotency bug and a stale in-script DNS guide caught by /code-review; a broader sweep of the whole privcloud CLI found and fixed three more real issues.
- Also ran /docs-builder reorg on docs/, moving 6 PRDs into a product bucket and generating a docs index; reported process findings back to a peer session.

## Antigens

### High Confidence (loaded — applies every session)
_(none yet — no antigen has reached 5+ distinct sessions)_

### Medium Confidence (observing — not loaded)
- Never report a task, fix, or test as done/validated/tested until you have actually run the verification and observed the result - don't claim success from assumption. (evidence: 4 sessions — "did you validate all fixes and confirmed that no regression? ... how/when can i release now?")

### Low Confidence (needs more data)
- When drafting bio/profile/doc content on the user's behalf, include every item the user specified and verify nothing was dropped before presenting it as final. (evidence: 2 sessions)
- Don't prompt for OTP/2FA when the user has said they're already authenticated or prefers a different login method - use the remembered method. (evidence: 2 sessions)
- Answer status questions directly and simply (pass/fail, what's left, current location) - don't bury the answer in prose. (evidence: 2 sessions)
