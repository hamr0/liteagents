# Project Memory
> Auto-generated by /remember. Do not edit manually.

## Facts

- **Release/publish is manual, OIDC-gated.** CHANGELOG -> bump version (3 spots) -> test -> merge -> tag -> `gh release` -> manual `publish.yml`. Verify all 4 surfaces agree.
- **POC-first is strictly enforced.** Spike against REAL APIs, aimed at the riskiest assumption. Test must be able to FAIL. Prove, don't assert. Never ship the POC.
- **Never retrieve API keys directly** — user keeps them in `pass`. Ask the user to run with the key or key in interactively; the GPG cache expires between runs.
- **Canonical AGENT_RULES live in litectx** (`.claude/memory/AGENT_RULES.md`): shared dev standards — POC-first, Testing Trophy, security invariants, spec-before-build.
- **Architecture split (eval-assist):** bareagent is the meter (originates counts/cost), bareguard is the gate (decides). Dependency direction bareagent -> bareguard only.
- **litectx kinds are CLOSED** (`code`/`doc`/`fact`/`episode`) — extend via `format`+`scope`, never new kinds. No `ctx.scoped()`; restore verb is `get`, not `recall`.
- **litectx `enumerate` (0.26.0) is the rank-free exhaustive scan**, memory-axis only. `fact`/`episode` (mem) vs `code`/`doc` (doc) scope axes are separate — mixing throws.
- **litectx: never hand-copy the old scope SQL** — reuse `_resolveMemReadScope`/`_memFilter`, or it leaks see-all. Public ids come out as `path` via `memId()`.
- **Per-provider usage counts cache differently:** OpenAI/Gemini INCLUDE cached tokens in the prompt count; Anthropic EXCLUDES cache. Gemini needs a native provider.
- **Stack/env:** pure JS+JSDoc, `node:test`, flat `src/`, prefix naming. `npm run typecheck` gates CI/publish. Ollama runs via podman, not docker.
- **No flaky POC tests** — don't write tests designed to pass; test real failure modes with real data, not a fixture authored to contain the result.
- **bareguard is an optional peerDependency.** Core imports nothing; only `bin/cli.js` lazily requires it under `if (cfg.gate)`. Published `dependencies: {}` is the signal.
- **`HaltError` from a tool's `execute` must re-throw, not wrap to `ToolError`** (fixed 0.18.0) — every seam now propagates the clean governance exit.
- **Meter->gate pricing: forward cost AS-IS, never synthesize.** Non-finite cost -> `null`, never a number — `NaN >= cap` is `false`, silently disabling the budget cap.
- **`remember`'s `memory.facts` is disjoint from `memory.stored`** (writes without ctx) — a fact counts once, never double-counts.
- **Folding a transcript must satisfy 3 wire invariants:** tool_call/result pairing, role alternation, `tool_use.id` charset. An inline note is a synthetic tool-call+result pair.
- **Ship docs WITH the code** (CHANGELOG+CLAUDE.md+PRD, +README for adopter-facing) in the SAME release, not batched — batching nearly shipped a half-shipped feature.
- **`recurse()` is thin glue**, never a new engine, never imported by `loop.js`. Family-A (model decides via `spawn_child`) is default. Self-call is the default spawn.
- **`maxDepth` is a topology knob, NOT a guard** — bareguard via `ctx.depth->policy` is the only guard layer.
- **recurse retrieval = TASK-SHAPE routing (settled):** `count/all` -> scan (default); `needle` -> search (CANNOT count); `exact` -> FTS-AND. Search->count drops 75-95% recall.
- **Completeness-contract guard upgrades a capped search->scan on "how many/all" asks, never downgrades.** Aggregation is always CODE, never LLM arithmetic.
- **recurse `opts.synthesize`** = fn | `'concat'` | `'merge'`. A reduce override MUST gate on `childResults.length > 0`, else it clobbers a leaf's own answer with `0`.
- **recurse honesty:** a dead worker or incomplete child -> `{incomplete, missingSlices, best}`, never a silent sum. `assessComplexity` is a HINT except `simple`/`critical`.
- **recurse/RLM build is FULLY DONE (0.24.0).** Capability-matcher and auto-calibrated scan window POC-ruled-out; runaway fan-out closed by a wired bareguard gate.
- **recurse has NO intrinsic total-work cap by design** — bounding cost/burst is the GATE's job, not recurse's (documented in JSDoc/README/CLAUDE.md).
- **Temperature escalation (BA-8) only matters on temperature-ACCEPTING models.** On a fixed model critique alone carries recovery — critique is PRIMARY, temp SECONDARY.
- **`provider-temperature.js` graceful degrade (BA-10):** on a 400 naming temperature unsupported -> drop it, warn once, retry ONCE (keyed off error TEXT, never a model list).
- **A model-facing seam needs a cross-model smoke on the ACTUAL production model before "done"** — haiku-only validation missed a sonnet-breaking 400.
- **"Validate not assert" = re-run the live check AFTER review/security fixes land**, not reuse a pre-fix run.
- **Any unbounded gap (`.*`) in a regex over semi-trusted text is a ReDoS footgun** — bound every gap and prove the fix with a pathological-input test.
- **Sync a dep's "adopter integration" doc note only when THAT dep's own release changed a surface bareagent re-exposes** — not release-driven otherwise.
- **`opts.persona` (privileged system stance) vs `opts.context` (user run-state) are DISTINCT recurse seams** — don't launder cwd through persona.
- **`auditSafeCtx(ctx, overrides)`:** any wiring blob threaded as governance ctx must shallow-copy + strip live handles/secrets before it reaches a gate's audit record.
- **recurse carry-down knobs ride `forChild`'s spread**; top-only knobs are stripped at each boundary. `refineLeaf` SUMS token spend across attempts, never last-only.
- **`--test-force-exit` is HARMFUL — removed from tests.** On Node 22 it kills the process the moment the first batch settles, silently truncating counts.
- **A live verify-shipped-vs-POC replay catches what offline mutation tests can't** — a green mutation suite missed a prompt ambiguity that dropped real-model recall 0.93->0.29.
- **A governance policy deny is not a recoverable tool error** — the retry-spin bound belongs in the Loop. Short-circuit only on N consecutive denials, never the first.
- **A load-bearing guard claim requires a guard-OFF negative control** — "stopped early" alone doesn't prove the guard fired.
- **POC-first can correct a design, not just validate it** — a live spike can disprove an assumed failure mode and surface mapping specifics no source-read gives you.
- **Don't assert a fragile/non-deterministic property via a live test** — cover it with a deterministic mutation-tested unit test instead.
- **"Surface X" from an adopter often implies "and honor X downstream"** — an unconsumed field is dead weight.
- **CLAUDE.md is the MAP, not the LOG** — it changes only when a component's ROLE or a cross-cutting CONTRACT changes, never changelog-style.
- **Docs-sync must grep the feature-reference docs**, not just CHANGELOG/PRD — reference tables (error lists, capability tables) are easy to miss.
- **Before any irreversible publish/merge, ground the git/npm state first** — reality can move out-of-session. Verify npm-vs-main version parity as a release gate.
- **"Under-modeled boundary rounds OPTIMISTICALLY toward success" is a recurring bug class (BA-4/5/6/7/13/16-19/23/24).** Audit normalization AND enforcement separately.
- **A terminal/status signal must be ERROR-TAGGED when a consumer branches on `error` as the sole success signal** — never branch on falsiness of a payload field.
- **Anthropic `pause_turn` resume = re-send the PAUSED ASSISTANT TURN verbatim** — a bare `continue` restarts and re-pauses, spinning to the round cap.
- **`obj[key] || default` on a plain object with an untrusted key is a prototype-pollution footgun** — guard with `hasOwnProperty.call(table, key)`.
- **A "no findings" verdict is UNTRUSTWORTHY if verify agents died mid-run** — recover finders' raw candidates from the run journal and verify yourself.
- **BA-7 provider-native blocks: preserve OPAQUELY**, never re-serialize from parsed fields. Don't sell a protocol-fidelity fix as a capability win without measurement.
- **Narrowest-guard principle (BA-12): a spin detector counts the NARROWEST condition**, never broadly — broad counting reds recovering-with-varying-args controls.
- **A plausible-but-unvalidatable, pre-existing, no-data-loss finding ships as a DOCUMENTED open item** — not force-fixed, not silently dropped.
- **`assert.notMatch` does NOT exist in `node:test`** — use `assert.doesNotMatch`. A new PUBLIC surface bumps MINOR even in a bugfix release.
- **RSI-POC backlog CLOSED — three spikes, ZERO shipped code.** Reflection/default-flip REJECTED (marginal); sensor-gaming CONFIRMED (judge RETURNED result only).
- **Lean-primitive bar:** never add a param for a marginal effect — a build must BEAT the existing path on cost OR be cost-neutral-when-inert.
- **An elicitation NULL is meaningless unless the harness creates the exploit's PRECONDITION** — an unsatisfiable close is required to force it.
- **Never execute untrusted model-generated code in-process without a hard timeout** — run it isolated (`spawnSync {timeout}`). ~99% CPU + zero progress = infinite loop.
- **`createShellTools()` returns `{tools}`, NOT a bare array** — passing the wrapper into a Loop's `tools` slot silently gives the model no tools.
- **BA-14 rejectedBuffer and BA-8 temperature escalation are ANTAGONISTIC** — the buffer holds temperature flat once it engages; effect is weak-model-only.
- **`shell_edit` requires an EXACT-ONCE anchor** (0 or 2+ -> a recoverable refusal, not a throw). Measured 167.9x fewer output tokens than a whole-file write.
- **Atomic-write-via-temp-file has its OWN security vector** (symlink-follow + world-readable window) — harden with `O_EXCL` + `0o600` + temp-name entropy.
- **Validate a published npm artifact through its PUBLIC export surface ONLY** — an internal `_`-prefixed export is not the shipped contract.
- **`docs/product` holds the canonical PRDs** — condense new design docs into the decisions log as dated entries instead of side-docs.
- **When a merged side-doc is deleted, grep repo-wide for dangling references and repoint them.**
- **Cross-repo adopter ask-numbers can collide** — disambiguate by FEATURE NAME. A cross-repo ask is a SYMPTOM, not a DIAGNOSIS — verify the mechanism on the real wire.
- **`cmd && echo "clean"` is NOT proof a check passed** — a failing command can print nothing. Verify every "clean" claim by EXIT CODE, never string-matching.
- **Mutation-prove every fix (revert -> red -> restore), don't just re-run** — a harness can produce a false "reproduced" via a malformed stub.
- **Restore uncommitted work via `cp` to scratch, never `git checkout`** — it reverts tracked files to HEAD and can't restore untracked files at all.
- **A fix round is the least-reviewed code in the repo** — a second review pass right after is high-value. `/security` must re-run after the fix round too.
- **Any diagnostic/error string from an unknown-shape object must be allowlist-filtered + length-clamped** before riding into receipts/audit fields.
- **Classify faults by TYPED error (`instanceof`), never message-prefix parsing.** A status-only verdict must DERIVE `pass`, never mutate the caller's object.
- **`await` on a value with a throwing accessor fails INSIDE the `await`** — split "callee threw" from "return is unreadable" around the await itself.
- **Review yield tracks review DEPTH (finder count/effort), not code quality** — comparing counts across effort levels is NOT evidence of convergence.
- **Pre-register the worded readout for EVERY possible outcome before running a probe** — a first "null" is often no-headroom (ceiling effect), not a true null.
- **A branch fully determined by its input combinations is NOT a decision needing sign-off.**
- **A "park/assessed, not built" decision reasoned from an UNVERIFIED premise must be re-checked before being treated as settled.**
- **Grounding a starting state must include the base commit's DOCS/PRD resolution, not just its headline/merge status.**
- **A cleanup refactor's proof of non-regression is EXACT test-count parity**, not merely "still green" with a different number.
- **Merge-vs-squash is a per-release USER choice, not a fixed policy** — ask each release. On an admin-merge block, hand the exact command to the user.
- **A capability probe must mirror REAL-TASK shape** — the model must independently realize it needs a tool, not respond to a trivial "call this tool" instruction.
- **`claude -p` is full Claude Code with its entire tool surface, not a bare model endpoint** — reduce with `--tools '' --strict-mcp-config --setting-sources ''`.
- **Reddit posting via barebrowse:** refs shift between snapshots — re-snapshot before clicking. `Enter` in a title textbox inserts a newline, not submit.
- **Subreddit constraints:** r/LLMDevs needs disclosed affiliation; r/MachineLearning bans "marketing" posts; r/coolgithubprojects needs `[Language]` + 6mo repost gap.
- **CLIPipe has TWO tool modes chosen on COST, not capability** — native (default) is 8-16x cheaper than emulation. Output-quality parity deliberately unminted.
- **`Provider.ownsCycle` (BA-16): a cycle-owning provider's `subtype:'success'` is NOT a success signal** — track bridge health via attempted-vs-served counts.
- **`stream-json` emits ONE `assistant` event per CONTENT BLOCK (BA-17)**, repeating usage — inflates ~4-5x. A turn = a run of events sharing `message.id`.
- **Provider timeouts live in `src/provider-http.js`.** `timeoutMs` = socket-idle bound (resets on activity); `deadlineMs` = wall-clock ceiling idle can't catch.
- **The retry primitive is `Retry.call`, NOT `withRetry`.** Per-attempt `timeout` ABANDONS the promise without closing the socket (leaks) — not a substitute for `deadlineMs`.
- **BA-20 `judge()` ports bareguard's E6i verbatim** — floor tiebreak: cannot-confirm-honored -> `broke`. Truncation/`parseError` are DISTINCT outcomes floored to `broke`.
- **`judgeToAnnotation` is a PURE render of a judge verdict** into bareguard's `gate.annotate` sink; field budgets enforced by bareguard AND bounded defensively by the adapter.
- **Judge calibration: a tier is `admitted` only if the clear-case floor is met AND zero reds AND every injection style resisted.** Grade by UNANIMITY, NEVER majority.
- **Dead-knob antipattern: never thread a provider option the provider doesn't actually honor** — verify against provider SOURCE before trusting a caller-supplied override.
- **`build:types` is STRICTER than `tsc --checkJs` and gates publish** — run it in the ship gate, not just typecheck.
- **A new PUBLIC export minted for a peer/cross-repo ask still needs the local USER's explicit go** — don't unilaterally mint public surface on a peer's request.
- **Validating a "what did you gloss over" challenge means testing EVERY flagged item to a pass/fail verdict**, not just fixing the obvious ones and declaring done.
- **Pricing honesty (BA-21/22):** rates resolve caller `Loop({rates})` -> Claude tier -> Sonnet-ceiling default. `unpriced` fires ONLY on absent/non-finite usage.
- **BA-24 usage-null boundary CLOSED (7 sites, v0.39.0):** an http provider MUST emit `usage:null` on an ABSENT block, never manufactured zeros. `usage` is `Usage|null`.
- **A pricing-honesty fix must validate against the REAL provider shape, not a stub** — BA-23 relocated the laundering to the provider boundary instead of fixing the class.
- **Concurrent Claude sessions in the same repo dir SHARE one git tree/branch** — verify git state fresh before any commit; coordinate via SendMessage first.
- **Absorbing an unrelated commit already pushed onto your release branch is the USER's call** — force-pushing a shared, already-pushed branch needs explicit sign-off.

## Episodes

### 2026-08-24 — BA-24 usage-null boundary: code-review escalation, concurrent-session conflict, v0.39.0 shipped
- Goal: finish BA-24 (paused pending peer bareloop's F6 validation) — code-review the 6-site fix, then release.
- Spawned `/code-review medium --fix`: the 6 original sites were correct/mutation-proven, but escalated a 7th manufacture-from-absence site outside the original scope — `provider-clipipe.js`'s BA-16 native session-close, whose zero-turn `.reduce()` over an empty turn array still produced a truthy all-zeros `Usage`. Fixed + 2 regression tests, mutation-proven. Peer independently confirmed F6 against the real `AnthropicProvider` (3/3 null, 2/2 priced, e2e halt/no-halt), clearing the release pause.
- Mid-`/release`, hit a concurrent-session git-tree collision: a parallel `docs-test` session had pushed a docs-reorg commit onto the same shared branch. User decided to absorb it into 0.39.0 via merge-commit rather than split/force-push.
- Versioned 0.39.0 MINOR not 0.38.2 PATCH (`GenerateResult.usage` widened to `Usage|null`, a public-surface change). Full gates green, diff-review Ready:Yes, security clean; merged PR #35, tagged, published to npm `latest`; peer re-validated 2050/2050 against the published package.
- Lesson: a "done" scope should be re-escalated by review before release, not just re-run — the 7th site nearly repeated BA-23's exact one-layer miss; concurrent sessions sharing one working tree need fresh git-state verification before every commit.

### 2026-08-24 — BA-23 pricing-usage fix shipped (v0.38.1) + BA-24 provider usage-null built
- Goal: peer bareloop (F6 pricing-red halt) reported 0.38.0's Loop path prices a no-usage round as $0 instead of honest-null — `resolveRoundCost`'s null branch was DEAD because loop.js fed it the stale truthy `lastUsage` seed instead of `result.usage`.
- BA-23: fed `result.usage ?? null` to the resolver; +regression tests mutation-proven; two spawned-Sonnet `/code-review medium` passes CLEAN; peer validated F6; shipped v0.38.1 via spawned-Sonnet `/release`.
- Peer then found BA-23 was scoped too narrowly: EVERY http provider manufactures an all-zeros usage object on an absent block, so the null branch is unreachable on the paid path — the laundering had only moved up a layer. Reconciled the whole-class scope with the peer.
- BA-24 built on branch `fix/ba24-usage-null-boundary` (`hasUsageSignal` discriminator; `usage:null` on absent/empty block; cache-only stays priced): 1124 pass, typecheck + build:types exit 0, mutation-proven. NOT shipped this session — paused for peer F6-against-real-provider + code-review.
- Lesson: a cross-repo ask is a symptom not a diagnosis, and a pricing-honesty fix validated against a convenient stub just relocates the bug.

### 2026-08-23 — BA-21 pricing honesty + BA-22 native rateSource shipped (v0.38.0)
- Goal: price every token-bearing round off an honest guesstimate with a structured provenance signal, instead of silently refusing-or-zeroing on an unknown model.
- Built the rate-resolution chain (caller rates → recognized Claude tier → Sonnet-ceiling default) + `rateSource` on every metering payload, splitting `'tier'` (recognized guess) from `'default'` (blind ceiling); one loud per-instance guesstimate warn, silenced by passing `rates`. BA-22: native CLIPipe session-close stamps `rateSource:'provider'` from the CLI's own `total_cost_usd`.
- `unpriced` narrowed to fire ONLY on absent usage / non-finite arithmetic. Shipped v0.38.0; a package-lock inner-version resync was caught as a follow-up.
- Lesson: keep the user in the know — a guesstimated price is fine, a silently-invented one is not.

### 2026-08-12 — BA-20: decisive judge + calibration harness + judgeToAnnotation (SHIPPED v0.36.0)
- Goal: productize bareguard's measured E6i "decisive judge" as a bareagent primitive — verbatim request + one structured egress artifact → binary `honored|broke` + mechanical `where`.
- POC-first proved E6i reproduces through bareagent's HTTP `AnthropicProvider`. User then challenged "what did you gloss over? that's cheating" → an 8-item self-audit: 3 real defects fixed (dead `model`/`effort` knobs, single-flavor injection → 5-style battery, unknown-tier cost fabrication), 2 overclaims evidenced, 2 risks measured low, 1 correctly-documented.
- Cross-session reconciliation with a parallel bareguard-maintainer session verified every peer claim before acting, corrected a muddled docs claim (real sink shape is `{surface, verdict, where, meta}`), and built `judgeToAnnotation` only after the user's explicit go on the new public export.
- Shipped v0.36.0 via `/release`; a mid-flight bareguard-maintainer correction (narrowing the "facts survive" guarantee to exclude the redacted audit row) was folded into the release commit rather than papered over.
- Lesson: validating a "what did you miss" challenge means testing every flagged item to a pass/fail verdict, and a peer's contract correction mid-release gets folded in before shipping, never after.

### 2026-07-28 — BA-19: provider total-duration deadline shipped (v0.35.0)
- Goal: close bareloop's BA-19 — bound TOTAL call duration, since BA-18's idle `timeoutMs` can't catch a zombie stream (a real adopter run hung 274 minutes, ending in `ECONNRESET` not a timeout).
- Verified the gap was real in source, ran a mandatory POC (5/5, incl. a negative control proving idle genuinely can't catch a zombie stream), built `applyRequestDeadline`/`applyRequestBounds`. Code-review fixed a garbage `deadlineMs` silently resolving to disabled (→ throw `ValidationError`), and 4×-duplicated wiring (→ `applyRequestBounds`).
- Hit two repeat footguns: a mutation-harness `git checkout` restore wiped uncommitted work, and an `&&`-chained typecheck masked a real TS1016 that `/ship`'s exit-code check caught. Shipped v0.35.0.
- Lesson: a disabled-by-default safety knob must still fail loud on garbage, and `/security`+diff-review must re-run AFTER the fix round.

### 2026-07-26 — BA-18: provider request/idle timeout shipped (v0.34.0)
- Goal: fix bareloop's BA-18 — the http(s) providers had no request/idle timeout, so a dropped/hung socket blocked `generate()` up to ~2h instead of failing fast.
- Part 1 (no timeout) confirmed real via source read; part 2 ("`withRetry` has zero call sites") was a NAME mismatch — `Retry.call` already existed and was already wired. POC first against real local http servers, explicit user sign-off on scope (all 4 providers) and default (finite 600000ms).
- Built shared `src/provider-http.js`, wired into all 4, +21 tests mutation-proven, live verify-shipped 4/4. `/code-review` found 2 real bugs (null-inherit shadowing; NaN/negative silently disabling instead of fail-safe-defaulting). Shipped v0.34.0.
- Lesson: cross-repo asks need root-cause verification, and disable-edge params must fail toward safety.

### 2026-07-23 — BA-17: native turn-unit fix shipped (v0.33.1)
- Goal: resolve bareloop's BA-17 claiming the CLIPipe native turn bound miscounted tool-calls as turns and failed to enforce `--max-turns`.
- Investigation INVERTED the filed diagnosis: `--max-turns` enforces correctly and counts assistant turns. The actual bug was one layer up — `stream-json` emits one `assistant` event per content block, each repeating usage, so per-event `onTurn` inflated turns ~4.4× and tokens ~5.04×.
- Fixed via `message.id` adjacent-run turn dedup, a `>`-only parent-side overrun backstop, BA-5 `lastText` preservation, and floored per-tier token residual reconciliation. +34 tests, 19 mutations red/green, two independent live verify-shipped runs. Shipped v0.33.1.
- Lesson: an upstream-filed bug report is a symptom, not a diagnosis — re-verify the mechanism on the real wire before building to the stated cause.

### 2026-07-21 — BA-16: CLIPipe native MCP tool mode shipped (v0.33.0)
- Goal: replace v0.32.0's CLIPipe tool-call emulation (Loop re-sends the whole transcript each round) with a native mode for the claude CLI, which has its own MCP tool channel.
- Corrected the user's framing: the real 0.32.0 issue was an unmeasured COST tradeoff, not loop-ownership degrading output. A unix-socket-stub POC (6/6 green) proved gate/spin-guards/turn-bound/stop-classification/metering all survive at the provider's `tools/call` bridge seam at 8–16× lower cost.
- Built `src/provider-clipipe-mcp.js` + `src/mcp-bridge-stub.js`, wired `Provider.ownsCycle` into `loop.js`. Code-review caught a reentrancy bug (async `onTurn` corrupting the stream parse buffer) and a dead-bridge-still-reports-success gap (fixed via parent-side bridge health tracking). Shipped v0.33.0.
- Lesson: a cycle-owning provider is a distinct contract shape; "external terminal reports success while nothing worked" recurs at every new provider boundary.

### 2026-07-21 — v0.32.0 released: CLIPipe tool mode + Evaluator predicate fix
- Goal: finish and ship CLIPipe tool mode (agentic Loop off a Claude CLI subscription) plus the Evaluator predicate false-green fix, resumed after a `/clear`.
- User challenged the one carried-over "open decision" (warn-vs-throw with no `toolProtocol`) and was right: fully determined by inputs. Grounding it did surface a real residue — stale JSDoc claiming "throws" while the code warned. Fixed.
- `/code-review medium`: CLEAN, 2 low notes closed (documented the capability probe's invisible-to-gate cost; extracted a shared `mapClaudeMeta` helper). Proven behavior-preserving by exact 914-pass parity before/after.
- Ran full release gates, merged via merge-commit, tagged, published, verified all four surfaces at 0.32.0. Lesson: reversing a previously-parked decision is CORRECT once the parking rationale is shown to rest on a false premise.

### 2026-07-21 — CLIPipe tool mode + predicate fix built (v0.32.0, held pre-release)
- Goal: validate a cross-repo learnings doc's improvement ideas item-by-item before building, then act on whatever was still real.
- The doc was STALE (claimed BA-15 unmerged when shipped) and described the Evaluator predicate bug's failure direction BACKWARDS (claimed coerce-to-`false`; actually coerced to `true` — a false green). Correcting that premise reversed a prior session's "assessed, NOT built" park.
- Built both deliverables POC-first, catching a probe-design failure: a naive "call this tool" capability probe false-positived on haiku, reshaped to require independent tool-need realization. Chose schema-validated envelope emulation over an MCP callback specifically to keep the Loop owning round accounting.
- Outcome: 3 commits, version bumped, nothing pushed — release explicitly HELD per user instruction. Lesson: "validate, don't assert" extends to the assistant's own summary of finished work.

## Antigens

### High Confidence (loaded — applies every session)
- **Never report a fix/feature/task as done, tested, or solved without actually running verification and showing the evidence — asserting success is not validating it. Verify by EXIT CODE, not by string-matching printed output.** (evidence: **5 sessions** across bareloop, bareagent, 8een, latefyi — "did you validate not asseert?"; "you will fucking validate this after correction with fucking tests and regression and smoke test"; "no gaps, validate and verify"; and 2026-08-12 BA-20: "did you validate all changes since last time?" + "what did you gloss over? that's cheating" → an 8-item audit testing every flagged item to a pass/fail verdict.)

### Medium Confidence (observing — not loaded)
> Already a durable Fact; recorded here as a tracked antigen because recurrence is measurable, NOT loaded hot (needs ≥5).
- **Surface known gaps, missing capabilities, and process inefficiencies proactively — never let the user discover them later ("papering over").** (evidence: **3 sessions** across multis, 8een, bareloop — "stop here, and dont paper over it"; "no papering over missing capabilities")

### Low Confidence (needs more data)
- Friction (2026-08-24, post-BA-24-build) scanned the global sessions root → **69 clusters, ALL 1-session** — nothing reached Medium (≥3) or High (≥5). Re-scanned again (2026-08-24, post-BA-24-release/0.39.0) → **67 clusters, ALL 1-session**; re-scanned a 3rd time this run → same **67 clusters**, same hashes, zero new sessions — nothing promotes; the two Medium/High antigens above carry forward unchanged. Prior runs for comparison (2026-08-12: 58, 07-21: 49, 07-16: 47, 07-15: 43, 07-11: 34, 07-03: 29).
- Notable 2-session, single-project pattern worth watching: **"when an implementation has become a stack of ad-hoc patches, stop and redesign instead of adding another patch"** (2 sessions, multis only — confined to one project, so not generalized).
- Recurring one-off themes this round (each 1 session, recorded not promoted): follow the referenced spec file exactly rather than creating unrequested content; edit docs at their canonical origin, not a downstream copy; keep README to its genre (not changelog narrative); don't overstate an optional dependency as always-required; ship only actual diffs at release ("no diffs only, no fit to pass"); don't open a PR per change when told to push directly; stop re-raising items already verified closed.
- 1-session signal (2026-08-24): **scope a fix to the whole bug CLASS, not one layer per release** — reinforced twice now (BA-23→BA-24 provider-boundary relocation, and the BA-24 7th-site near-miss in the same release). Promoted into a durable Fact this round given the double recurrence within one project; watch for a 3rd cross-project instance before treating it as more than project-local.
