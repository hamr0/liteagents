# Project Memory
> Auto-generated by /remember. Do not edit manually.

## Facts

- **Release/publish flow is manual and OIDC-gated.** CHANGELOG cut → version bump in **3 spots** (`package.json`, `package-lock.json`, `bareagent.context.md` header line) → full test → merge to main → tag → `gh release` → then **manually** trigger `publish.yml` via `workflow_dispatch`. The release does NOT auto-publish (by design — don't make it). All four surfaces must agree: local git, GitHub branch+tag, Release page, npm.
- **POC-first is strictly enforced.** Validate logic with a ~15-min spike against **REAL APIs** before building; aim the spike at the *riskiest assumption*, not the easy part; the test must be able to FAIL (real uncrafted data, can show the negative). "Prove, don't assert" — a POC's output is evidence you ran, not prose. Never ship the POC.
- **Never retrieve API keys directly.** User keeps them at `pass amr/openai_api`, `pass amr/claude_api`, `pass amr/gemini_api`. Ask the user to run with the key (via `!`) or have them key in interactively / authorize. The GPG passphrase cache expires between runs, so `pass` may time out non-interactively.
- **Canonical AGENT_RULES live in the litectx repo:** `/home/hamr/Documents/PycharmProjects/litectx/.claude/memory/AGENT_RULES.md` (shared dev standards: POC-first, Testing Trophy, security invariants, spec-before-build).
- **Architecture split (eval-assist):** bareagent is the **meter** (originates counts: turns, tools, tokens, cost via `estimateCost`), bareguard is the **gate** (consumes emissions to decide). Dependency direction is bareagent → bareguard (bareguard is provider-agnostic, never makes an LLM call). This is why the Evaluator/refine must live in bareagent.
- **litectx is at ≥0.26.0** (devDep bumped `^0.16.0`→`^0.26.0` at RLM step 7; suite stayed green @679). Kinds are CLOSED (`code`/`doc`/`fact`/`episode`) — extend via `format`+`scope`, never new kinds. Isolation = a separately-scoped `new LiteCtx({ owner, session })` (§4.4) or `spawn` — there is **no `ctx.scoped()` method**. Lossless stash restore verb is **`get`**, not `recall`. `count({kind})` ships 0.23.0 (per-kind, tenant-fenced, expiry-aware); reuse `_resolveMemReadScope`/`_memFilter`, never hand-copy the old `(:me IS NULL OR …)` SQL (it leaks see-all); `fact`/`episode` (mem axis) vs `code`/`doc` (doc axis) are SEPARATE scope axes — `recentMemory`/`count` throw if mixed; public ids come out as `path` via `memId()` (W4). **`enumerate` SHIPPED in 0.26.0** — exhaustive, scope-aware, paged, **rank-free** scan (the one read litectx couldn't do — every read was FTS-gated); v1 = memory axis only (`fact`/`episode`), code/doc deferred. DoD verified bareagent-side before wiring (`poc/litectx-enumerate-verify.mjs`).
- **Per-provider token-usage normalization differs (verified live):** OpenAI `prompt_tokens` and Gemini `promptTokenCount` INCLUDE cached tokens → subtract for the uncached remainder; Anthropic `input_tokens` EXCLUDES cache → use as-is. Gemini needs a **native** provider — its OpenAI-compat endpoint omits the cache tier. Anthropic doesn't auto-cache (needs `cacheSystem`/`cache_control`); OpenAI + Gemini auto-cache.
- **Stack/env:** pure JS + JSDoc, `node:test` (zero deps), flat `src/` with prefix naming (`provider-*.js`, `store-*.js`). `npm run typecheck` (tsc --checkJs) gates CI and publish; `.d.ts` are generated/git-ignored. Ollama runs via **podman** (not docker) on this machine. Integration tests gated on `OPENAI_API_KEY`/`ANTHROPIC_API_KEY`.
- **No flaky POC tests** — don't write tests designed to pass; test real failure modes (prefer real data over a fixture authored to contain the result).
- **bareguard is an optional peerDependency (≥0.19.0).** The core imports nothing; only `bin/cli.js` lazily `require`s bareguard under `if (cfg.gate)`. Published manifest `dependencies: {}` is the canonical signal.
- **`HaltError` from a tool's `execute` must re-throw, not wrap to `ToolError`** (fixed 0.18.0). It was the one seam that swallowed it; every seam now propagates the clean governance exit.
- **Meter→gate pricing: forward `costUsd`/`pricing` AS-IS, never synthesize.** Non-finite cost (±Inf/NaN) → `null` (unpriced), never a number — `NaN >= cap` is `false`, which silently DISABLES the budget cap.
- **`remember` writes `kind:'fact'` via the generic 4-verb Store socket (no litectx coupling); `memory.facts` is disjoint from `memory.stored`** (remember writes without ctx → a fact counts once, never double-counts).
- **Folding the live transcript must satisfy 3 wire invariants:** tool_call/tool_result pairing, Anthropic user/assistant alternation (no consecutive-role merge), and `tool_use.id` charset `^[a-zA-Z0-9_-]+$`. An inline note must be a synthetic `assistant(tool_call)+tool(result)` PAIR (a bare note breaks alternation; a `system` note clobbers the system prompt). The provider tool-name separator is `_`, never `.`.
- **Ship docs WITH the code** (CHANGELOG + CLAUDE.md + PRD status, and for adopter-facing changes context/README too) in the SAME commit/release, not batched at the end — batching nearly committed a half-shipped feature.
- **`recurse()` (RLM primitive) = thin glue** composing Loop/spawn/assessComplexity/Planner/runPlan/Evaluator — never a new engine; never imported by `loop.js`. **Family-A is the default** ("B-shell with an A-tool": the shell offers a `spawn_child` tool, the model decides decomposition); Family-B forced fan-out is opt-in (`opts.count`/`mode:'fanout'`, throws until step 5). **In-process self-call is the default spawn mechanism** (~0 ms/node vs ≥90 ms/node for a process fork — fork reserved for heavyweight delegation). **`maxDepth` is a topology knob** (stops OFFERING spawn; `=1` ⇒ flat), **NOT a guard** — bareguard via `ctx.depth→policy` is the only guard layer (recurse adds no second one).
- **recurse retrieval = TASK-SHAPE routing (RC-5, SETTLED on AG News §9.2.1 — do not re-run pull/flat/search):** `count/"all"` → `opts.retrieval:'scan'` (DEFAULT) = scan every slice + LLM-judge + **CODE-count** (window≈8 recall-knee, passes=2 shuffled-boundary union → recall 0.93/precision 0.98); `needle` → `'search'` (opt-in) = litectx `recall`, embeddings on, `fact`/`episode`, capped `KNN_K=8` — **CANNOT count**; `exact` → `'exact'` (opt-in) = FTS-AND/code filter, embeddings off. litectx `recall` is FTS-gated; embeddings genuinely NOMINATE only for `fact`/`episode`. "Search→count" silently drops 75–95% recall (retrieval is structurally capped). Completeness-contract guard upgrades a capped search→scan on "how many/all" asks, **never** downgrades. Aggregation is **always CODE**. `recurse` is **litectx-agnostic** — depends only on the slice-source shape (`Slice[] | () => Promise<Slice[]>`); `litectxCorpus(litectx,{kind})` paginates `enumerate` (one adapter, not the default). Data-driven *width*: `mode:'partition'` measures the corpus → `width=max(opts.count floor, ⌈size/workerBudget⌉)` parallel scan-workers (capped by guards+size), union-counted. Locked numbers (don't re-derive): scan `window=8`/`passes=2`, `KNN_K=8`, `DEFAULT_WORKER_BUDGET=100`, tier→count `{1,2,4,6}`.
- **recurse synthesis = code-reduce for aggregation** (LLM arithmetic carried ~10–15% error even at full retrieval). `opts.synthesize` = a function (deterministic code-reduce over child `results`) | `'concat'` | `'merge'` (isolated Loop). A reduce override MUST gate on `childResults.length > 0`, else it clobbers a leaf's own answer with `0` (a real caught bug).
- **recurse honesty invariants:** RC-9 — a dead/guard-exhausted worker OR an incomplete *child* → `{incomplete, missingSlices, best}`, **never a silent survivor-sum** (propagate up the tree, on BOTH the code-reduce and model-synthesis paths). RC-2 copy-on-return — a child sees only its subtask (fresh msg array); only the result STRING crosses back. `assessComplexity` is a HINT, not a gate — only `simple→single-shot` and `critical→force-verify` are non-overridable (the critical floor is a safety the model must not talk past). `Loop.run()` returns no `model` field → read `provider.model` for receipts.
- **recurse/RLM build is FULLY DONE (as of 0.24.0).** PRD §10 steps 3–8 + the per-query scan-as-a-tool follow-on (`retrieval:'tools'`, `buildScanTool`) + `mode:'partition'` resident e2e all closed. Two §11 deferrals were POC-ruled-out (RC-4 capability matcher: scoping buys ~0 routing gain at any toolset size; auto-calibrated scan window: no reproducible recall knee); the weak-SLM runaway-fan-out concern was closed by PROOF (not assertion) that a wired bareguard gate (budget+maxTurns) cuts an ungoverned 43–117-call runaway to 4–5 calls / clean incomplete.
- **recurse has NO intrinsic total-work cap by design** — Family-A model-driven spawn can reach `HARD_ROUND_LIMIT(100) × maxDepth`; bounding cost/burst is the GATE's job, not recurse's (documented in JSDoc + README + CLAUDE.md).
- **Temperature escalation (BA-8) is load-bearing ONLY for temperature-ACCEPTING models.** On a temperature-fixed model (`claude-sonnet-5`, OpenAI o1/gpt-5-class → 400 on any non-default temperature) the fed-back **gap critique carries recovery alone** — critique is the PRIMARY refine lever, temperature is SECONDARY diversity (proven live: sonnet converges flat-temp via the critique).
- **`src/provider-temperature.js` graceful degrade (BA-10, all 4 providers):** on a 400 whose message names `temperature` as unsupported AND a temperature was sent → drop it, warn once per instance, retry ONCE — keyed off API error TEXT, never a model list; a genuine out-of-range 400 still re-throws. `GenerateResult.temperatureDropped` threads provider→Loop→recurse; `receipts.refineLeaf.temperatures` records EFFECTIVE temps (a dropped attempt = `null`, never the ignored requested value).
- **Toy fixtures on ONE model hide production-model integration bugs** — a model-facing seam must get a cross-model smoke on the ACTUAL production model before "done" (haiku-only BA-8 validation missed a sonnet-breaking 400). Corollary: **"validate delivery not assert" = re-run the live verify-shipped check AFTER review/security fixes land**, not reuse a pre-fix run.
- **Any unbounded gap (`.*`) in a regex over semi-trusted text (provider/proxy error messages) is a quadratic-blowup (ReDoS) footgun** — bound every gap (e.g. `[^.]{0,40}`) and prove the fix with a pathological-input test that would hang if unbounded.
- **Per-dependency "adopter integration" doc notes are surface-change-driven, NOT release-driven** — only sync a dep's integration note when THAT dep's own release changed a surface bareagent re-exposes.
- **`opts.persona` vs `opts.context` are DISTINCT recurse seams.** persona = privileged SYSTEM stance (augments not replaces, carries down, EXCLUDED from the isolated verifier/scan judge); context (BA-9) = USER-message run-state (paths/cwd), carries down AND shown to the verifier as neutral FACTS. Don't launder cwd through persona.
- **`auditSafeCtx(ctx, overrides)` (recurse.js, BA-1/F16):** any primitive threading a wiring blob as governance ctx MUST shallow-copy + strip live handles/secrets (`provider`/`apiKey`) before it reaches a gate's verbatim `_ctx` audit record — bareguard's own redaction is value/pattern-based + opt-in, so it can't be relied on for a key placed on ctx (provider NAME still reaches the audit for identity).
- **recurse carry-down knobs ride `forChild`'s `...opts` spread** (persona, context, refineLeaf, tools, synthesize, maxDepth); top-only knobs (contract/evaluate/count/mode/retrieval/corpus) are stripped at each child boundary. A multi-Loop receipts node (`refineLeaf`) SUMS token spend across attempts, never last-only (the gate sees each via `onLlmResult` regardless).
- **`--test-force-exit` is HARMFUL — removed from `npm test`/`test:unit`.** On Node 22 it kills the process the moment the first batch settles, abandoning async-heavy tests still in flight (they don't pass/fail/count): `recurse.test.js` reported 31 vs the true 40, `test:unit` 530 vs 535 — a silent, timing-dependent truncation. The original "hang" it guarded was a `| tail` buffering artifact; the full suite completes cleanly without it.
- **A live verify-shipped-vs-POC replay catches what offline mutation tests structurally cannot.** Step 8: a 56-test mutation suite (all green) missed a classify-prompt ambiguity that dropped real-model recall 0.93→0.29 (a scripted judge can't be *confused* by a prompt the way a real model is). Always run the shipped primitive against real data on any new retrieval/LLM path before claiming done — the offline suite proves the wiring, not the prompt.
- **A governance policy deny is not a recoverable tool error** — the retry-spin bound belongs in the Loop (bareguard is stateless per `check()`), not the gate. Short-circuit only on N **consecutive** denials (default 3, reset on any passing tool call) — never on the first deny, since a single deny must stay advisory to preserve allowlist-safe pivoting.
- **A load-bearing guard claim requires a guard-OFF negative control** alongside the positive verify-shipped test — a "stopped early" observation alone doesn't prove the guard fired; it must be contrasted against the same scenario with the guard disabled.
- **POC-first can correct a design, not just validate it** — run the live spike before locking the mechanism; it can disprove an assumed failure mode (e.g. interleaving) and simplify the fix. Corollary: **it can also surface mapping specifics no source-read gives you** — always run the real target CLI/API before building a parser/mapping, since a pasted sample under-specifies edge cases (e.g. object-keyed usage fields, suffix-bearing model ids).
- **Don't assert a fragile/non-deterministic property** (e.g. a model choosing to pivot) via a live test — cover it with a deterministic mutation-tested unit test instead.
- **"Surface X" from an adopter often implies "and honor X downstream."** Complete the whole data path (e.g. a new field must actually be preferred/consumed where it matters), not just expose the field — an unconsumed field is dead weight.
- **CLAUDE.md is the MAP, not the LOG.** Never update it changelog-style; it changes only when a component's ROLE or a cross-cutting CONTRACT changes. New-feature narrative belongs in CHANGELOG/JSDoc/types instead.
- **Docs-sync on any change must grep the feature-reference docs** (e.g. `docs/02-features/*.md`), not just CHANGELOG/context/PRD — those reference tables (error-message lists, capability tables) are easy to miss and are a real "docs done" gap.
- **Before any irreversible publish/merge action, ground the git/npm state first** (`gh pr list`, `npm view versions`, tags, package.json-vs-npm) rather than trusting the in-session mental model — reality can move out-of-session (e.g. another PR merged and bumped the version without this session's involvement). A merged-but-unpublished version bump is a real failure mode — verify npm-vs-main version parity as a release-gate step, not an afterthought.
- **The "under-modeled boundary rounds OPTIMISTICALLY toward success" bug class (BA-4/5/6/7/13/16/17/18/19/23/24 family) is a recurring category.** (BA-16: a dead bridge still ends `subtype:'success'`; BA-17: one message ≠ one stream event; BA-18: a hung socket reads as "still working"; BA-19: garbage at a disable-edge silently disables the bound; BA-23/24: an absent usage block gets coalesced into a manufactured $0-priced object.) At a normalize/represent boundary the external side reports something the neutral shape can't hold — or the Loop surfaces but never ACTS on — and the code rounds monotonically toward success/done/empty/no-cap. An optimistic error looks exactly like success so nobody debugs it. Audit question: *"what can the real API say here that our neutral shape has no slot for — or that the Loop surfaces but never acts on — and which way does it round?"* Normalization and enforcement are SEPARATE failure points — audit both.
- **A classified terminal/status signal must be ERROR-TAGGED, not merely surfaced, when a downstream consumer branches on `error` as the sole success signal** (BA-13: `recurse` keys on `out.error`, never `out.stopReason` — surfacing alone leaves a refused worker laundering as converged; erroring propagates honest `{incomplete}` with ZERO consumer changes). Corollary: **branch on the explicit `error` signal, never on falsiness of a payload field** — `out.text || fallback` is a trap once upstream stops guaranteeing empty-on-failure.
- **Anthropic `pause_turn` resume = re-send the PAUSED ASSISTANT TURN verbatim** (partial text + `server_tool_use` blocks) so the server continues where it left off. A bare `continue` re-sends byte-identical input → the server restarts and re-pauses → the loop spins to the round cap (WORSE than the original laundering bug it "fixed"). `server_tool_use` blocks are DISTINCT from `tool_use` and ride the BA-7 `providerBlocks` passthrough, not normalized `toolCalls`. SDK tool-runners don't auto-resume pause_turn — you push the paused turn back yourself.
- **`obj[key] || default` on a plain object literal with an untrusted/provider-passed key is a prototype-pollution footgun** — `toString`/`constructor`/etc. resolve to inherited `Object.prototype` functions (truthy). Guard with `Object.prototype.hasOwnProperty.call(table, key)`. Bites wherever a value that normalization passes through verbatim reaches a table lookup, and can recur MULTIPLE times in one file — a release-gate diff-review is a valid second net for the same bug class.
- **A code-review / workflow "no findings survived verification" verdict is UNTRUSTWORTHY if its verify agents died mid-run** (spend limit, API error) — recover the finders' raw candidates from the run journal (`journal.jsonl`) and verify them yourself. N finders converging on one defect that the dead verifiers never adjudicated is a real finding, not "no findings."
- **BA-7 provider-native block passthrough: preserve OPAQUELY** (keep bytes, never re-serialize from parsed fields — a `redacted_thinking` block can't survive that, and a `type==='thinking'` check silently drops the next new block type = the same bug again), **replay only on provider+model tag match** (else drop to the lossy-but-valid prior request, never a 400). A protocol-fidelity fix must NOT be sold as a capability/outcome win without a measured head-to-head — ship it labeled honestly ("no measured benefit") when that's the evidence, and bake that constraint into the PRD acceptance criteria so it can't be quietly re-sold.
- **Narrowest-guard principle (BA-12): a spin/stuck detector counts the NARROWEST triggering condition** (byte-identical repeats: `name`+`JSON.stringify(args)`), never broadly (any consecutive failure) — broad counting reds the negative controls (a model varying args while recovering; alternating tools), punishing the recovery it exists to protect. A guard's trigger is narrow, NOT a proxy for the general failure class — shipping one guard can open a NEW distinct spin surface (a tool ERROR ≠ a policy DENIAL, so the deny-streak guard doesn't bound tool-error spins → BA-12). And count EVERY path that feeds the same error back (BA-12 had to count both the `execute`-throw and the unknown/hallucinated-tool branch via one shared `recordToolFailure`).
- **A plausible-but-unvalidatable, pre-existing (NOT a regression), no-data-loss code-review finding ships as a DOCUMENTED open item** — not force-fixed, not silently dropped ("no papering over"). Honesty about the gap satisfies the bar without blocking the release.
- **Small durable facts:** `assert.notMatch` does NOT exist in `node:test` — use `assert.doesNotMatch`. A branch that adds new PUBLIC surfaces (a new `GenerateResult`/`Message` field, a new constructor option) bumps **MINOR** even when the rest of the same release is bugfixes. A receipt/flag threaded onto returns (e.g. `temperatureDropped`) must ride EVERY terminating path (halt/deny/stop/hard-limit), not just the clean/truncated ones, or an upstream honest-reporting seam silently breaks.
- **RSI-POC backlog CLOSED (2026-07-16) — three validation spikes, ZERO shipped code.** §2.A structured reflection → REJECTED (marginal, weak-model-only, +26% tokens, costlier than the buffer). §2.C sensor-gaming → CONFIRMED (sonnet gamed an unsatisfiable side-effect close 5/5 — edited the test AND made `solve` stateful — the `refineLeaf.sensor` "judge the RETURNED result, never a gameable side-effect" note is now demonstrated-in-repo, not just borrowed). §2.B BA-14 default-flip → REJECTED (on `findDiagonalOrder`, escalate+critique 100% beat flat+buffer 80% — REVERSE of task 1's 16/16-vs-3/6; a within-model reversal ⟹ TASK-SPECIFIC, not universal). Parked: §2.D rubric leaf-close. Out of scope for `recurse`: §2.E cost receipt, #3 stateless-control, #6 memorization-auditor.
- **Lean-primitive bar for a new param/knob:** never add one for a marginal, narrowly-scoped, or costlier-than-existing effect (it dilutes the primitive) — a build must BEAT the existing path on cost OR be cost-neutral-when-inert (the BA-14 adaptive-engage pattern: activates only once a condition fires, free when inert). This decided all three RSI-POC no-builds.
- **An elicitation/red-team NULL is meaningless unless the harness creates the exploit's PRECONDITION** — a probe that never puts the model under exploit-motivating pressure can't distinguish "won't game" from "was never tempted" (§2.C's first truthful-close run was a weak null; only an UNSATISFIABLE close, where green ⟹ tampering, forced the exploit). Pair with a tamper-proof control (run the RETURNED artifact vs hidden cases in isolation).
- **Never execute untrusted model-generated code in-process** (`new Function`/`eval`) without a hard timeout — run it in an ISOLATED child process (`spawnSync {timeout}`) so a hang becomes a scored FAIL fed back as critique, not a wedged run. Diagnostic tell: **~98–99% CPU + zero progress = an infinite loop executing** (a slow API call is near-zero CPU while waiting). Cloning a POC harness carries the OLD task's hidden assumptions.
- **`createShellTools()` returns `{tools}` (an object wrapper), NOT a bare array** — passing the wrapper straight into a Loop's `tools` slot silently gives the model no usable tools (0 tool calls, a real harness confound).
- **BA-14 `refineLeaf.rejectedBuffer` and BA-8 temperature escalation are ANTAGONISTIC** (directed vs random diversity) — measured monotonic degradation as temp rises (flat 0.2 → 100%, 0.7 → 70%, 1.0 → 50%), so the buffer HOLDS temperature flat at `temps[0]` when it engages. Adaptive default: engages only after a prior attempt's `temperatureDropped` fired (a temp-fixed model where escalation is inert); byte-identical on temp-accepting models. Efficacy is a weak-model/fixation phenomenon (honest null on sonnet), hence adaptive-not-always-on. `refine.js`'s `attempt` callback gained a `history` field (defensive copy of prior `{result, verdict}`) — the seam that made it possible.
- **`shell_edit` (v0.29.0, anchored exact-string replace):** literal `indexOf`+`slice` splice (never `String.replace` — `$&`/`$1` in `newText` would corrupt); requires an EXACT-ONCE anchor (0 or 2+ → a recoverable tool-RESULT refusal naming the count, NOT a throw — so a repeated miss does NOT feed `maxIdenticalToolErrors`, bounded only by maxTurns/budget). Gated by `fs.writeScope` via `{type:'edit'}` (bareguard already treats `edit`=`write` in `FS_TYPES`, zero gate change). Measured **167.9× fewer OUTPUT tokens** than a whole-file `shell_write` on a one-line edit (800-line file, sonnet).
- **Atomic-write-via-temp-file introduces its OWN security vector** (symlink-follow at the temp path + a world-readable creation window before a later chmod) — harden with `O_EXCL` (`flag:'wx'`) + restrictive creation mode (`0o600`) + sufficient temp-name entropy. Hardening a vector your own change created is in-scope, not gold-plating.
- **Validate a published npm artifact through its PUBLIC export surface ONLY** — an internal/test-only `_`-prefixed export is not the shipped contract and must not be used to "prove" a release works.
- **`docs/product` holds the canonical PRDs** (`prd.md`, `rlm-prd.md`, plus `eval-assist-prd.md` where present) — condense new feature/audit design docs into the decisions log (`docs/wiki/decisions-log.md`) as dated entries instead of accumulating side-docs; when a merged side-doc is deleted, grep repo-wide for dangling path references and repoint them. (2026-08-24 reorg moved `docs/{00-context,01-product,02-features,03-logs,04-process}` → `docs/{product,logs,archive}` + wiki pages; see `project_prd_consolidation.md`.)
- **Cross-repo adopter ask-numbers can collide** (bareloop "BA-13" ≠ bareagent-internal "BA-13"; disambiguate by FEATURE NAME, e.g. `shell_edit`, never reuse the adopter's number) — and **a cross-repo upstream ask is a SYMPTOM report, not a root-cause DIAGNOSIS**: verify the actual mechanism in source / on the real wire before building to the ask's stated cause (recurred at BA-17, BA-18, and BA-23→BA-24's relocated-not-fixed laundering).
- **`cmd && echo "clean"` is NOT proof a check passed** — a failing command prints nothing and the silence reads as false success. Verify every "clean"/"no findings" claim by actual EXIT CODE, never by string-matching printed output.
- **Mutation-prove every fix (revert → red → restore), don't just re-run.** A plain re-run hides both a confounded validation harness and a test that was defensively rewritten to stop failing rather than to prove the fix. Corollary: a validation harness can produce a false "reproduced" when a malformed stub ends the run before the code path under test is reached — debug the harness before trusting a degenerate result. Restore of UNCOMMITTED work under a mutation harness must use working-copy backups (`cp` to scratch), never `git checkout` (which reverts TRACKED files to HEAD and silently wipes the fix under test; an UNTRACKED file can't be checkout-restored at all).
- **A fix round is the least-reviewed code in the repo** — a second review pass immediately AFTER a fix round is high-value, not redundant. And **`/security` must be re-run after the fix round**, not only before it: a fix can introduce its own new vector (the diagnostic-string→audit-log leak class, F16/BA-1 family).
- **Any diagnostic/error string built from a caller-supplied or thrown object of unknown shape must be allowlist-filtered to conventional fields AND length-clamped** before riding into `receipts`/`blockerDetail` — it can reach a plaintext audit log verbatim through a wired gate (a non-Error throw via `JSON.stringify` leaked into a 200K-char string).
- **Classify faults by TYPED error (`instanceof`), never by message-prefix/magic-string parsing.** A status-only `{status:'satisfied'}` verdict must DERIVE `pass` (never mutate the caller's object) — `refine.js` stops only on `.pass`, so an unmapped status-only verdict silently never terminates. `broken-*` outranks `governance-deny` when both apply (a fault in the caller's own code is more actionable than a governance block).
- **`await` on a value with a throwing accessor/getter fails INSIDE the `await`** (the engine's own `.then` probe) — code separating "the callee threw" from "its return is unreadable" must put that split around the `await` itself, not around later property access.
- **Review yield tracks review DEPTH (finder count / effort setting), not code quality** — comparing finding counts round-over-round across different effort levels (`medium` vs `xhigh`) is NOT evidence of convergence.
- **Pre-register the worded readout for EVERY possible outcome (EFFECT / NULL / NO-HEADROOM / WRONG-SIGN) before running a probe** — it prevents post-hoc rationalizing a null into a claimed win. A probe's first "null" is often **no headroom** (strong/easy-tier model, ceiling effect), not a true null — the harness must create the precondition before a null is meaningful.
- **A branch fully determined by its input combinations is NOT a decision needing user sign-off.** Only genuinely underdetermined tradeoffs warrant a checkpoint; over-flagging a settled branch as an "open decision" wastes a round-trip.
- **A "park"/"assessed, NOT built" decision reasoned from an UNVERIFIED premise must be re-checked before being treated as settled** — don't let "already assessed" calcify into "settled" without re-verifying the underlying claim.
- **Grounding a starting state must include the base commit's DOCS/PRD resolution sections, not just its headline/merge status** — a commit silently carries more resolved decisions than its subject line suggests.
- **A cleanup refactor's proof of non-regression is EXACT test-count parity** (same N pass before and after), not merely "still green" with a different number.
- **Merge-vs-squash is a per-release USER choice, not a fixed policy** — ask each release rather than assuming the previous release's strategy carries forward. `gh pr merge --admin` can be blocked by the permission classifier; on block, stop and hand the exact command to the user rather than working around it.
- **A capability probe must mirror REAL-TASK shape** — it must require the model to independently realize it needs a tool. A trivial "call this tool" instruction-style probe is a FALSE POSITIVE (haiku passes 4/4 yet fails real tool tasks 0/5). Keep detection behaviour-based, never a model-name roster (BA-10).
- **`claude -p` is full Claude Code with its entire built-in tool surface, not a bare model endpoint** — reduce it to a "bare brain" with `--tools '' --strict-mcp-config --setting-sources ''` before treating it as a scriptable generator. `--setting-sources ''` alone is a measured ~18× input-token cut (37,423 → 2,026 tokens/turn) because the CLI otherwise auto-loads the cwd's `CLAUDE.md`/memory EVERY turn.
- **Reddit posting via barebrowse (operational):** refs shift between snapshots — re-snapshot immediately before clicking or the click silently no-ops; old-reddit flair options aren't in the ARIA `act` tree (use `pruneMode:'read'`); `Enter` in a title textbox inserts a newline instead of submitting; a logged-out link-post page prunes its title (pruning artifact, not removal — verify via the sub's `/new`); `browse` is stateless and separate from the `goto` session; Reddit blocks incognito `.json` fetches; `**` wrapped around a `` `code` `` span renders as literal asterisks.
- **Subreddit constraints for repo posts:** r/LLMDevs requires DISCLOSED affiliation (undisclosed → permanent ban; carve-out for free/open projects); r/MachineLearning bans/purges "strategic marketing campaign" posts and rejects bare arXiv links without commentary; r/coolgithubprojects requires `[Language]` in the title + 6-month repost gap; the bareagent account is permanently banned from r/opensource. The `/post-repo` PRD §2 (venue selection + cooldown) is UNWRITTEN.
- **CLIPipe has TWO tool modes chosen on COST not capability:** emulation (`toolProtocol:'claude'`, v0.32.0 — Loop keeps cycle ownership, re-sends the transcript each round, ~$0.25–0.55/round) vs native (`toolProtocol:'claude-mcp'`, v0.33.0/BA-16 — DEFAULT for the claude CLI; CLI owns its own multi-turn session over a unix-socket MCP bridge back into bareagent closures, ~$0.006/turn, 8–16× cheaper). Output-quality parity is deliberately UNMINTED (n=2) — the shipped claim is COST ONLY.
- **`Provider.ownsCycle` contract (BA-16):** when a provider runs its own multi-turn session inside one `generate()`, the Loop's per-round seams can't run — the Loop THROWS at construction if `assemble`/`trim`/`cacheMessages`/a Loop-level `policy` are set alongside it (never a silently-dead knob); governance moves to the provider's `tools/call` bridge (same policy + BA-11 deny-streak + BA-12 identical-error + turn bound). A cycle-owning provider's own `subtype:'success'` is NOT a success signal — a dead MCP bridge still ends `success` while every tool call failed; track bridge health PARENT-SIDE via attempted-vs-served tool-call counts.
- **Claude Code `stream-json` emits ONE `assistant` event per CONTENT BLOCK (BA-17), each repeating that message's `usage`** — per-event turn/token accounting inflates ~4–5×. A turn = a RUN of consecutive events sharing `message.id` (adjacent-run dedup; no-id degrades to one-turn-per-event). Reconcile via `subtractUsage(total, streamed)` floored at 0 (over-feed the budget, never under-feed). `--max-turns` counts ASSISTANT turns and enforces correctly but is UNDOCUMENTED (CLI 2.1.216) → guard with a live tripwire + a parent-side overrun backstop firing on `>` only (never `>=`).
- **Provider request timeouts live in shared `src/provider-http.js` across all 4 http providers.** BA-18 `timeoutMs` = socket-IDLE bound (`req.setTimeout`, resets on any activity, `code:'ETIMEDOUT'`/`retryable:true`, FINITE **600000ms** default, fail-SAFE to default on garbage). BA-19 `deadlineMs` = an absolute NON-resetting wall-clock ceiling for a "zombie stream" that idle CANNOT catch (`code:'EDEADLINE'`/`retryable:false` TERMINAL; `context.bound:'idle'|'deadline'`; idle trips FIRST when `timeoutMs < deadlineMs`; DISABLED by default; **THROWS `ValidationError` on garbage** since a disabled-default knob has no safe fallback). `applyRequestBounds(req, {timeoutMs,deadlineMs}, name)` is the single per-`_request` seam.
- **The retry primitive is `Retry.call` (`src/retry.js`), NOT `withRetry`** — already wrapped around `provider.generate` in `loop.js` + `run-plan` `stepRetry`; `DEFAULT_RETRY_ON` classifies `ETIMEDOUT`/`ECONNRESET`/`ENOTFOUND`/429/5xx transient. `Retry.call`'s per-attempt `timeout` (Promise.race, default 60000ms) ABANDONS the promise without closing the socket (leaks) — not a substitute for the provider-level `deadlineMs`.
- **BA-20 judge (`src/judge.js`, SHIPPED v0.36.0):** `judge({request, artifact, provider, maxTokens?=512, onLlmResult?}) → {verdict:'honored'|'broke', where, truncated, parseError, costUsd, usage, model, raw}`. Ports bareguard's E6i `judgeDecisive` VERBATIM (floor tiebreak: cannot-confirm-honored → `broke`; artifact-as-untrusted-data; decisive binary, never a confidence scale). Truncation/`parseError` are DISTINCT outcomes floored to `broke`, EXCLUDED from calibration denominators. `costUsd` reds an unknown tier to `null`. NO per-call `model`/`effort` (dead knobs). Injection resistance established on `claude-haiku-4-5` ONLY (re-calibrate per tier). Composes AROUND a provider; `loop.js` never imports it.
- **`judgeToAnnotation(verdict, opts?)` (`src/bareguard-adapter.js`, BA-20):** a PURE render of a judge verdict → bareguard's `gate.annotate` sink `{surface, verdict, where, meta}` (NOT the pre-E6 `{kind,...,text}` sketch). `surface = verdict !== 'honored'` (load-bearing fail-open). Field budgets enforced SILENTLY by bareguard (verdict ≤80 chars, where ≤300, **meta ≤1000 bytes ALL-OR-NOTHING**) — the adapter bounds DEFENSIVELY at both points (visible `…[clipped]` marker) via `opts.limits`. Guarantee scope: the "facts survive the meta ceiling" promise holds for the DRAINED fact + humanChannel EVENT only, NOT the persisted AUDIT ROW under a configured redactor (redaction runs downstream and can still blow the audit line's cap, replacing the whole meta with `{_truncated}`/`{_unserializable}` — check both markers).
- **bareguard peer-range coordination RESOLVED (v0.36.1):** bareguard 0.13.0 (hardens `gate.annotate()` to reject a malformed fact into an `annotate_malformed` audit row) went live → bare-agent widened peer+dev ranges to `>=0.9.0 <0.14.0`, no code change needed (adapter already compliant: explicit-boolean `surface`, `String()`-coerced JSON-safe `meta`, no `__proto__`). 0.13.0 also redacts `verdict` in the audit line and strips `__proto__` at every depth.
- **Judge calibration harness (`src/judge-calibration.js`, BA-20):** a tier is `admitted` only if the clear-case floor is met AND zero reds AND every injection style resisted. Clear-case set is a VENDORED, hash-pinned bareguard fixture (`test/fixtures/e6i-cases.frozen.json`); a 5-style `INJECTION_BATTERY` (forged/override/fake-system/role-confusion/reassurance) is a SEPARATE admission gate; `constantHonored` is the negative control that MUST fail. Grade by UNANIMITY over usable samples, NEVER majority. An acceptance report must NAME the fixture hash it graded.
- **Dead-knob antipattern: never thread a provider option the provider doesn't actually honor** (`model`/`effort` on `AnthropicProvider.generate`, which builds from `this.model` and reads neither) — verify against provider SOURCE before accepting a caller-supplied override as meaningful.
- **`build:types` (JSDoc→`.d.ts`) is STRICTER than `tsc --checkJs` typecheck and gates publish** — a `@property {Function}` can pass typecheck yet fail `.d.ts` emission against a named interface signature; run `build:types` in the ship gate, not just typecheck.
- **A new PUBLIC export minted for a peer / cross-repo ask still needs the local USER's explicit go** — don't unilaterally mint public surface because another repo's maintainer requested/assigned it. Corollary: **"bound at both points" is NOT duplication** when the second bound is the last code before a silent, never-throwing, clipping sink.
- **Validating a "what did you gloss over / that's cheating" challenge means testing EVERY flagged item to a pass/fail verdict** — fix the real defects, evidence the reporting overclaims, measure the over-flagged-as-low, correct the mislayered — not fixing the obvious ones and declaring done.
- **Pricing honesty (BA-21/22):** every token-bearing round is priced off a guesstimate — rates resolve caller `Loop({rates})` → recognized Claude tier (`haiku`/`sonnet` substring) → Sonnet-ceiling default; `rateSource:'provider'|'caller'|'tier'|'default'|null` rides every metering payload; `unpriced` fires ONLY on absent usage or non-finite arithmetic, never on an unknown model; a guesstimated round emits one LOUD `console.warn` per Loop instance (silence by passing `rates`). Native CLIPipe session-close stamps `rateSource:'provider'` from the CLI's own `total_cost_usd` (BA-22).
- **BA-24 usage-null boundary CLOSED (7 sites, v0.39.0 shipped 2026-08-24):** an http provider MUST emit `usage:null` when the raw usage block is ABSENT/empty — never a manufactured all-zeros object, which launders an unpriceable round into a confident $0-priced one and defeats a downstream pricing-red halt. Per-field `|| 0` stays correct when the block IS present (a cache-only round stays priced). `hasUsageSignal(block,keys)` (`src/provider-usage.js`) is the discriminator; `GenerateResult.usage` is `Usage|null` (public-surface change → MINOR bump). Site #7 — BA-16 CLIPipe native session-close, a zero-turn `.reduce()` over an empty turn array still producing a truthy all-zeros object — was found by code-review escalating PAST the original 6-site scope; `subtractUsage` guarded null-safe. Rule #2 (resolveRoundCost all-zero→unpriced) is OUT — its false-fire is a live halt on the adopter's side (arbiter territory).
- **A pricing-honesty fix must be validated against the REAL provider output shape end-to-end, not a convenient `usage:null` stub the real provider can't emit** — BA-23 fixed only the `loop.js` symptom layer, relocating the laundering one layer up to the provider boundary (→BA-24); the BA-24 7th site nearly repeated the exact same one-layer miss, caught only because review escalated explicitly. Coalescing the ABSENCE of a whole block into a default object is the bug class; per-field `|| 0` on a PRESENT block stays correct.
- **Concurrent Claude sessions in the same repo directory SHARE one git working tree/index/checked-out branch** — a peer's staged/committed/pushed work can land on your release branch mid-flight. Verify git state fresh (status/log/origin) before any commit during multi-session work, coordinate via SendMessage before touching shared files, and treat a peer's "tree is clean" as possibly having become true via a commit+push you didn't witness.
- **Absorbing an unrelated commit already pushed onto your release branch is the USER's call** (include-in-release vs. split-and-force-push) — force-pushing a shared, already-pushed branch is destructive and needs explicit sign-off, never a unilateral choice.

## Episodes

### 2026-08-24 — BA-24 usage-null boundary: code-review escalation, concurrent-session conflict, v0.39.0 shipped
- Goal: finish BA-24 (paused pending peer bareloop's F6 validation) — code-review the 6-site fix, then release.
- Spawned `/code-review medium --fix`: the 6 original sites were correct/mutation-proven, but escalated a 7th manufacture-from-absence site outside the original scope — `provider-clipipe.js`'s BA-16 native session-close, whose zero-turn `.reduce()` over an empty turn array still produced a truthy all-zeros `Usage`. Fixed + 2 regression tests, mutation-proven. Peer independently confirmed F6 against the real `AnthropicProvider` (3/3 null, 2/2 priced, e2e halt/no-halt), clearing the release pause.
- Mid-`/release`, hit a concurrent-session git-tree collision: a parallel `docs-test` session had pushed a docs-reorg commit onto the same shared branch. User decided to absorb it into 0.39.0 via merge-commit rather than split/force-push.
- Versioned 0.39.0 MINOR not 0.38.2 PATCH (`GenerateResult.usage` widened to `Usage|null`, a public-surface change). Full gates green, diff-review Ready:Yes, security clean; merged PR #35, tagged, published to npm `latest`; peer re-validated 2050/2050 against the published package.
- Lesson: a "done" scope should be re-escalated by review before release, not just re-run — the 7th site nearly repeated BA-23's exact one-layer miss; concurrent sessions sharing one working tree need fresh git-state verification before every commit.

### 2026-08-24 — BA-23 pricing-usage fix shipped (v0.38.1) + BA-24 provider usage-null built
- Goal: peer bareloop (F6 pricing-red halt) reported 0.38.0's Loop path prices a no-usage round as $0 instead of honest-null — `resolveRoundCost`'s null branch was DEAD because loop.js fed it the stale truthy `lastUsage` seed instead of `result.usage`.
- BA-23: fed `result.usage ?? null` to the resolver; +regression tests mutation-proven; two spawned-Sonnet `/code-review medium` passes CLEAN; peer validated F6; shipped v0.38.1 via spawned-Sonnet `/release`.
- Peer then found BA-23 was scoped too narrowly: EVERY http provider manufactures an all-zeros usage object on an absent block, so the null branch is unreachable on the paid path — the laundering had only moved up a layer. Reconciled the whole-class scope with the peer.
- BA-24 built on branch `fix/ba24-usage-null-boundary` (`hasUsageSignal` discriminator; `usage:null` on absent/empty block; cache-only stays priced): 1124 pass, typecheck + build:types exit 0, mutation-proven. NOT shipped this session — paused for peer F6-against-real-provider + code-review.
- Lesson: a cross-repo ask is a symptom not a diagnosis, and a pricing-honesty fix validated against a convenient stub just relocates the bug.

### 2026-08-23 — BA-21 pricing honesty + BA-22 native rateSource shipped (v0.38.0)
- Goal: price every token-bearing round off an honest guesstimate with a structured provenance signal, instead of silently refusing-or-zeroing on an unknown model.
- Built the rate-resolution chain (caller rates → recognized Claude tier → Sonnet-ceiling default) + `rateSource` on every metering payload, splitting `'tier'` (recognized guess) from `'default'` (blind ceiling); one loud per-instance guesstimate warn, silenced by passing `rates`. BA-22: native CLIPipe session-close stamps `rateSource:'provider'` from the CLI's own `total_cost_usd`.
- `unpriced` narrowed to fire ONLY on absent usage / non-finite arithmetic. Shipped v0.38.0; a package-lock inner-version resync was caught as a follow-up.
- Lesson: keep the user in the know — a guesstimated price is fine, a silently-invented one is not.

### 2026-08-12 — BA-20: decisive judge + calibration harness + judgeToAnnotation (SHIPPED v0.36.0)
- Goal: productize bareguard's measured E6i "decisive judge" as a bareagent primitive — verbatim request + one structured egress artifact → binary `honored|broke` + mechanical `where`.
- POC-first proved E6i reproduces through bareagent's HTTP `AnthropicProvider`. User then challenged "what did you gloss over? that's cheating" → an 8-item self-audit: 3 real defects fixed (dead `model`/`effort` knobs, single-flavor injection → 5-style battery, unknown-tier cost fabrication), 2 overclaims evidenced, 2 risks measured low, 1 correctly-documented.
- Cross-session reconciliation with a parallel bareguard-maintainer session verified every peer claim before acting, corrected a muddled docs claim (real sink shape is `{surface, verdict, where, meta}`), and built `judgeToAnnotation` only after the user's explicit go on the new public export.
- Shipped v0.36.0 via `/release`; a mid-flight bareguard-maintainer correction (narrowing the "facts survive" guarantee to exclude the redacted audit row) was folded into the release commit rather than papered over.
- Lesson: validating a "what did you miss" challenge means testing every flagged item to a pass/fail verdict, and a peer's contract correction mid-release gets folded in before shipping, never after.

### 2026-07-28 — BA-19: provider total-duration deadline shipped (v0.35.0)
- Goal: close bareloop's BA-19 — bound TOTAL call duration, since BA-18's idle `timeoutMs` can't catch a zombie stream (a real adopter run hung 274 minutes, ending in `ECONNRESET` not a timeout).
- Verified the gap was real in source, ran a mandatory POC (5/5, incl. a negative control proving idle genuinely can't catch a zombie stream), built `applyRequestDeadline`/`applyRequestBounds`. Code-review fixed a garbage `deadlineMs` silently resolving to disabled (→ throw `ValidationError`), and 4×-duplicated wiring (→ `applyRequestBounds`).
- Hit two repeat footguns: a mutation-harness `git checkout` restore wiped uncommitted work, and an `&&`-chained typecheck masked a real TS1016 that `/ship`'s exit-code check caught. Shipped v0.35.0.
- Lesson: a disabled-by-default safety knob must still fail loud on garbage, and `/security`+diff-review must re-run AFTER the fix round.

### 2026-07-26 — BA-18: provider request/idle timeout shipped (v0.34.0)
- Goal: fix bareloop's BA-18 — the http(s) providers had no request/idle timeout, so a dropped/hung socket blocked `generate()` up to ~2h instead of failing fast.
- Part 1 (no timeout) confirmed real via source read; part 2 ("`withRetry` has zero call sites") was a NAME mismatch — `Retry.call` already existed and was already wired. POC first against real local http servers, explicit user sign-off on scope (all 4 providers) and default (finite 600000ms).
- Built shared `src/provider-http.js`, wired into all 4, +21 tests mutation-proven, live verify-shipped 4/4. `/code-review` found 2 real bugs (null-inherit shadowing; NaN/negative silently disabling instead of fail-safe-defaulting). Shipped v0.34.0.
- Lesson: cross-repo asks need root-cause verification, and disable-edge params must fail toward safety.

### 2026-07-23 — BA-17: native turn-unit fix shipped (v0.33.1)
- Goal: resolve bareloop's BA-17 claiming the CLIPipe native turn bound miscounted tool-calls as turns and failed to enforce `--max-turns`.
- Investigation INVERTED the filed diagnosis: `--max-turns` enforces correctly and counts assistant turns. The actual bug was one layer up — `stream-json` emits one `assistant` event per content block, each repeating usage, so per-event `onTurn` inflated turns ~4.4× and tokens ~5.04×.
- Fixed via `message.id` adjacent-run turn dedup, a `>`-only parent-side overrun backstop, BA-5 `lastText` preservation, and floored per-tier token residual reconciliation. +34 tests, 19 mutations red/green, two independent live verify-shipped runs. Shipped v0.33.1.
- Lesson: an upstream-filed bug report is a symptom, not a diagnosis — re-verify the mechanism on the real wire before building to the stated cause.

### 2026-07-21 — BA-16: CLIPipe native MCP tool mode shipped (v0.33.0)
- Goal: replace v0.32.0's CLIPipe tool-call emulation (Loop re-sends the whole transcript each round) with a native mode for the claude CLI, which has its own MCP tool channel.
- Corrected the user's framing: the real 0.32.0 issue was an unmeasured COST tradeoff, not loop-ownership degrading output. A unix-socket-stub POC (6/6 green) proved gate/spin-guards/turn-bound/stop-classification/metering all survive at the provider's `tools/call` bridge seam at 8–16× lower cost.
- Built `src/provider-clipipe-mcp.js` + `src/mcp-bridge-stub.js`, wired `Provider.ownsCycle` into `loop.js`. Code-review caught a reentrancy bug (async `onTurn` corrupting the stream parse buffer) and a dead-bridge-still-reports-success gap (fixed via parent-side bridge health tracking). Shipped v0.33.0.
- Lesson: a cycle-owning provider is a distinct contract shape; "external terminal reports success while nothing worked" recurs at every new provider boundary.

### 2026-07-21 — v0.32.0 released: CLIPipe tool mode + Evaluator predicate fix
- Goal: finish and ship CLIPipe tool mode (agentic Loop off a Claude CLI subscription) plus the Evaluator predicate false-green fix, resumed after a `/clear`.
- User challenged the one carried-over "open decision" (warn-vs-throw with no `toolProtocol`) and was right: fully determined by inputs. Grounding it did surface a real residue — stale JSDoc claiming "throws" while the code warned. Fixed.
- `/code-review medium`: CLEAN, 2 low notes closed (documented the capability probe's invisible-to-gate cost; extracted a shared `mapClaudeMeta` helper). Proven behavior-preserving by exact 914-pass parity before/after.
- Ran full release gates, merged via merge-commit, tagged, published, verified all four surfaces at 0.32.0. Lesson: reversing a previously-parked decision is CORRECT once the parking rationale is shown to rest on a false premise.

### 2026-07-21 — CLIPipe tool mode + predicate fix built (v0.32.0, held pre-release)
- Goal: validate a cross-repo learnings doc's improvement ideas item-by-item before building, then act on whatever was still real.
- The doc was STALE (claimed BA-15 unmerged when shipped) and described the Evaluator predicate bug's failure direction BACKWARDS (claimed coerce-to-`false`; actually coerced to `true` — a false green). Correcting that premise reversed a prior session's "assessed, NOT built" park.
- Built both deliverables POC-first, catching a probe-design failure: a naive "call this tool" capability probe false-positived on haiku, reshaped to require independent tool-need realization. Chose schema-validated envelope emulation over an MCP callback specifically to keep the Loop owning round accounting.
- Outcome: 3 commits, version bumped, nothing pushed — release explicitly HELD per user instruction. Lesson: "validate, don't assert" extends to the assistant's own summary of finished work.

## Antigens

### High Confidence (loaded — applies every session)
- **Never report a fix/feature/task as done, tested, or solved without actually running verification and showing the evidence — asserting success is not validating it. Verify by EXIT CODE, not by string-matching printed output.** (evidence: **5 sessions** across bareloop, bareagent, 8een, latefyi — "did you validate not asseert?"; "you will fucking validate this after correction with fucking tests and regression and smoke test"; "no gaps, validate and verify"; and 2026-08-12 BA-20: "did you validate all changes since last time?" + "what did you gloss over? that's cheating" → an 8-item audit testing every flagged item to a pass/fail verdict.)

### Medium Confidence (observing — not loaded)
> Already a durable Fact; recorded here as a tracked antigen because recurrence is measurable, NOT loaded hot (needs ≥5).
- **Surface known gaps, missing capabilities, and process inefficiencies proactively — never let the user discover them later ("papering over").** (evidence: **3 sessions** across multis, 8een, bareloop — "stop here, and dont paper over it"; "no papering over missing capabilities")

### Low Confidence (needs more data)
- Friction (2026-08-24, post-BA-24-build) scanned the global sessions root → **69 clusters, ALL 1-session** — nothing reached Medium (≥3) or High (≥5). Re-scanned again (2026-08-24, post-BA-24-release/0.39.0) → **67 clusters, ALL 1-session** — still nothing promotes; the two Medium/High antigens above carry forward unchanged. Prior runs for comparison (2026-08-12: 58, 07-21: 49, 07-16: 47, 07-15: 43, 07-11: 34, 07-03: 29).
- Notable 2-session, single-project pattern worth watching: **"when an implementation has become a stack of ad-hoc patches, stop and redesign instead of adding another patch"** (2 sessions, multis only — confined to one project, so not generalized).
- Recurring one-off themes this round (each 1 session, recorded not promoted): follow the referenced spec file exactly rather than creating unrequested content; edit docs at their canonical origin, not a downstream copy; keep README to its genre (not changelog narrative); don't overstate an optional dependency as always-required; ship only actual diffs at release ("no diffs only, no fit to pass"); don't open a PR per change when told to push directly; stop re-raising items already verified closed.
- 1-session signal (2026-08-24): **scope a fix to the whole bug CLASS, not one layer per release** — reinforced twice now (BA-23→BA-24 provider-boundary relocation, and the BA-24 7th-site near-miss in the same release). Promoted into a durable Fact this round given the double recurrence within one project; watch for a 3rd cross-project instance before treating it as more than project-local.
